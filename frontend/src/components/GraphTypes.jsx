import CustomNode from './CustomNode'; // Replace with your actual path
import CustomEdge from './CustomEdge'; // Replace with your actual path

export const nodeTypes = {
  customNode: CustomNode,
};

export const edgeTypes = {
  customEdge: CustomEdge,
};
